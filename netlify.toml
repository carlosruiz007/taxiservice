[build]
  command = "npm run build"
  publish = "dist"

# Only rewrite GET/HEAD for the SPA so booking form POST reaches Netlify Forms.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = {Method = ["GET", "HEAD"]}
